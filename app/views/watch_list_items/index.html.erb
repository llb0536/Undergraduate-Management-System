<h1>Listing watch_list_items</h1>

<table>
  <tr>
    <th>Student</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @watch_list_items.each do |watch_list_item| %>
  <tr>
    <td><%= watch_list_item.student_id %></td>
    <td><%= link_to 'Show', watch_list_item %></td>
    <td><%= link_to 'Edit', edit_watch_list_item_path(watch_list_item) %></td>
    <td><%= link_to 'Destroy', watch_list_item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Watch list item', new_watch_list_item_path %>
