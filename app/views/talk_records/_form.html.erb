<%= form_for(@talk_record) do |f| %>
  <% if @talk_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@talk_record.errors.count, "error") %> prohibited this talk_record from being saved:</h2>

      <ul>
      <% @talk_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br />
    <%= f.text_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :about %><br />
    <%= f.text_field :about %>
  </div>
  <div class="field">
    <%= f.label :memo %><br />
    <%= f.text_area :memo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
